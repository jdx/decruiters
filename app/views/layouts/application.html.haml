!!!
%html
  %head
    %title decruiters.com
    = stylesheet_link_tag    "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .navbar
      .navbar-inner
        = link_to "Decruiters", root_path, class: 'brand'
        %ul.nav
          %li.active= link_to "Home", root_path
          - if logged_in?
            %li= link_to "Logout", '/logout'
            Logged in as #{current_user}
          - else
            %li= link_to "Login", '/auth/linkedin'
    .container
      = yield
